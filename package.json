{
  "name": "pcsrt",
  "version": "0.0.1",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun . -- -v",
    "build": "bun build --compile --minify --sourcemap --target=bun-linux-arm64 . --outfile build/pcsrt"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@inquirer/prompts": "^8.1.0",
    "chalk": "^5.6.2",
    "zx": "^8.8.5"
  }
}
